#include <iostream>
using namespace std;
int main()
{
    int length, width;
    cin >> length;
    cin >> width;
    cout << length * width << endl;
    cout << (length + width) * 2 << endl;
    return 0;
}